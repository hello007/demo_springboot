spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/liu
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode:  always #never #embedded #always